# FedAvg

**Axis:** Transfer / adaptation strategy &nbsp;·&nbsp; **Paradigm:** MI &nbsp;·&nbsp; **Dataset:** BNCI2014001 (cross-subject LOSO)

**Stage configuration:** `EA · no-aug · EEGNet · Linear · FedAvg`

## Mechanism
Federated averaging: each subject trains locally and the server averages the model weights. It is the vanilla federated baseline that isolates FedBS's two additions.

## Result — 3 seeds (1, 2, 3)

| Metric | Value |
|---|--:|
| Accuracy (mean ± std across seeds) | 74.54 ± 0.79 |
| Reference range (expected band) | 70–80 |

Accuracy is the mean over seeds; ± is the standard deviation *across* seeds (a reproducibility figure, not the cross-subject spread). The reference range is the published / expected band on this dataset.

## Provenance
- **Paper / source:** B. McMahan et al., AISTATS, 2017
- **Implementation:** Implemented against the Strategy ABC in src/hustbciml/algorithms/strategies/; the federated protocol is simulated in a single process (see the _fed.py docstring for what that does and does not demonstrate).

## Note
FedAvg in the Transfer Learning table. Regression band around this benchmark's own three-seed measurement; no directly comparable published cross-subject LOSO number exists for this protocol.

---
_Generated by `scripts/build_cards.py` from `tests/repro/repro_targets.yaml` + `docs/cards/_content.yaml`. Numbers mirror [RESULTS.md](../../RESULTS.md); terms are defined in [glossary.md](../glossary.md)._
