# EA-TIEEEGNet

**Axis:** Network (backbone) &nbsp;·&nbsp; **Paradigm:** MI &nbsp;·&nbsp; **Dataset:** BNCI2014001 (literal-nested-cross-subject-loso LOSO)

**Stage configuration:** `EA · no-aug · TIEEEGNet · Linear · ERM`

## Mechanism
TIE-EEGNet is EEGNet with its first temporal convolution replaced by a time-information-enhanced (TIE) convolution: a fixed sinusoidal time-positional embedding is added to the trial before temporal filtering, injecting explicit within-trial timing information that plain EEGNet lacks. Everything downstream — the depthwise spatial convolution, separable convolution and linear head — is unchanged, so it is a controlled one-block change to the backbone. Caveat (domain fit): TIE-EEGNet was developed for seizure subtype classification, not motor imagery — its time-positional design targets seizure EEG and may not be well-suited to MI, consistent with it only matching the EEGNet baseline here.

## Result — 5 seeds (1, 2, 3, 4, 5)

| Metric | Value |
|---|--:|
| Accuracy (mean ± std across seeds) | 72.95 ± 1.17 |
| Δ vs baseline (EEGNet, 72.76) | +0.19 |
| Reference range (expected band) | — |

Accuracy is the mean over seeds; ± is the standard deviation *across* seeds (a reproducibility figure, not the cross-subject spread). The reference range is the published / expected band on this dataset.

## Provenance
- **Paper / source:** Validated Network campaign; executable source d7a46f7c04da53bda0679b5f834815c5bfe1212d3163018abc5e77293bda138f
- **Implementation:** Refactored from Peng et al. 2022 (IEEE TNSRE) onto the Backbone ABC. The paper targets seizure subtype classification; here the architecture is evaluated on the same EA + ERM motor-imagery setup as the other backbones, at the benchmark EEGNet capacity (F1 4 / F2 8; the paper's F1 8 / F2 16 is the reference config). See the upstream repository for its original license terms.

## Note
Five-seed subject-macro accuracy from literal target-isolated nested LOSO. The complete campaign passed checkpoint reload, prediction, identity, cache, numerical-family, and coverage validation before publication.

---
_Generated by `scripts/build_cards.py` from `tests/repro/repro_targets.yaml` + `docs/cards/_content.yaml`. Numbers mirror [RESULTS.md](../../RESULTS.md); terms are defined in [glossary.md](../glossary.md)._
