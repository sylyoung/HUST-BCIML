# DAN: EA-aligned EEGNet trained with source cross-entropy plus a multi-kernel
# MMD between source and target features (Long et al., ICML 2015).
aligner: EA
augmenter: Identity
backbone: EEGNet
head: Linear
strategy: DAN
epochs: 100
batch_size: 32
lr: 0.001
